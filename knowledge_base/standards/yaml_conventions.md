# YAML Conventions
- Always use hierarchical properties for Spring Kafka configuration.
- `spring.kafka.streams.application-id` is mandatory.
- Define topics explicitly under custom properties if not using standard bindings.
- Topics must match the naming conventions.
