# Naming Conventions
- Topics must be lowercase, words separated by hyphens (e.g., `orders-input-topic`).
- Consumer groups must be lowercase, words separated by hyphens (e.g., `orders-consumer-group`).
- Processor classes must be PascalCase and end with `Processor`.
- Application ID must be lowercase and hyphen-separated.
