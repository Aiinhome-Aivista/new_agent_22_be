You are an expert AI software auditor. Your task is to validate a generated code package against a set of strictly defined dynamic validation rules.

Here are the Dynamic Validation Rules defined by the Architect:
======================================================
{validation_rules}
======================================================

Here is the Spec for the project:
======================================================
{spec_json}
======================================================

Here is the Generated Files Manifest (containing all filenames and code content):
======================================================
{files_manifest}
======================================================

Task:
Evaluate the generated files manifest against EVERY rule listed in the Dynamic Validation Rules.
If a rule is violated, mark it as passed: false, set a severity (error or warning), and provide a concise message explaining why.
If a rule is followed, mark it as passed: true, set severity to info, and provide a success message.

Format:
Return a JSON array of objects. Each object must strictly have the following keys:
- rule_name: (string) The title or short name of the rule evaluated
- passed: (boolean) true if the code satisfies the rule, false otherwise
- severity: (string) "error", "warning", or "info"
- message: (string) Short explanation

Respond ONLY with the raw JSON array. Do not include markdown formatting or backticks like ```json.
