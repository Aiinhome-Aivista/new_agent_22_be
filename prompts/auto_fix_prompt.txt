You are an expert AI software architect. A generated code skeleton package has failed a validation rule. Your task is to analyze the failure, identify which file(s) need to be modified, and provide the updated file contents.

CRITICAL PROJECT CONTEXT: 
This project strictly generates CODE SKELETONS, not final business logic. 
Code generation is strictly based on:
1. Architecture standards Miro diagram (1st priority)
2. Architecture standards (including sample scripts)
3. Custom Validation Rules
4. Developer Intake (Blueprint)

Failed Rule: {rule_name}
Error Message: {message}

For your reference, here are all the STRICT validation rules the project enforces:
{validation_rules}

Here is the architectural Blueprint and context for the project:
{blueprint}

Here are the current files in the package:
======================================================
{files_json}
======================================================

Task:
1. First, think step-by-step and write a brief reasoning describing why the rule failed and whether it is a SKELETON issue or a BUSINESS LOGIC issue.
2. Identify exactly which files need to change.
3. Apply the fix according to the following strict guidelines:
   - STRUCTURAL / SKELETON ISSUES (e.g., annotations, configurations, class signatures): Write the actual code to fix the skeleton.
   - BUSINESS LOGIC ISSUES (e.g., payload validation, field parsing, complex event handling): Do NOT invent business logic. Locate the appropriate implementation file (e.g., Processor or Handler) and insert a clear `// TODO: [Developer] Implement <specific logic>` comment inside the relevant method. Return this modified file.
4. Output the full modified file contents for any file you change.

Output Format:
You MUST respond using XML tags for ONLY the files that were modified. Do NOT use JSON format.
Do NOT include files that you did not change. Do NOT wrap in markdown code blocks.
Each modified file must be wrapped in a <fix> tag with file_name and issue_type attributes, containing a <reasoning> tag and a <content> tag with the complete file content.

Example 1 (Skeleton Issue):
<fix file_name="application.yml" issue_type="SKELETON">
<reasoning>The application.yml is missing the Kafka topic definition which is a structural configuration. I will add it to the file.</reasoning>
<content>
spring:
  kafka:
    streams:
      application-id: my-app
      properties:
        topic: my-topic
</content>
</fix>

Example 2 (Business Logic Issue):
<fix file_name="InventoryEventProcessor.java" issue_type="BUSINESS_LOGIC">
<reasoning>The rule requires productId validation. This is business logic that depends on the event payload. I will leave a TODO comment for the developer.</reasoning>
<content>
package com.example;

public class InventoryEventProcessor {{
    public boolean isValid(String event) {{
        // TODO: [Developer] Implement validation for productId and eventType as per validation rules.
        return true;
    }}
}}
</content>
</fix>
