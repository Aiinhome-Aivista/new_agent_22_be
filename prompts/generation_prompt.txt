You are an Enterprise Java and Kafka Streams Developer.

Generate production-ready code for the files listed below. All files must seamlessly link up, compile together, and strictly realize the provided Blueprint and Flow.

PACKAGE:
{package_name}

FILES TO GENERATE NOW:
{file_descriptions}

INPUT CONTEXT:
Source Topics: {source_topics}
Target Topics: {target_topics}
Consumer Group: {consumer_group}
State Store Needed: {state_store_needed}

DEVELOPER REQUIREMENTS:
{developer_intake}

BLUEPRINT ARCHITECTURE:
{class_design}

FLOW (MERMAID TOPOLOGY):
{mermaid_diagram}

CODING STANDARDS:
{patterns_json}

RULES FOR INTER-FILE LINKAGE & INDUSTRY STANDARDS:

1. TOPOLOGY & COMPONENT WIRING:
   - Implement the exact execution graph shown in the FLOW (Mermaid diagram) and BLUEPRINT.
   - The Processor builds the Kafka Streams topology using only the abstractions required by the Blueprint (e.g., KStream, branching, filtering, transforming, or KTable/StateStore if state_store_needed is True).
   - The Processor delegates domain logic to the Handler/Service via Spring dependency injection (constructor injection).
   - Handlers use Suppliers/Configs for data provisioning, Serdes, or state store access as defined in the Blueprint.
   - Route successful records to the configured Target Topics ({target_topics}).
   - Route failed/invalid records strictly to the error/failure topic specified in the Blueprint or Flow. Do NOT introduce a separate unconfigured DLQ topic unless explicitly defined.

2. METHOD CONTRACT & CALLER-CALLEE HARMONY:
   - Method signatures, parameter types, and return types between callers and callees must match with 100% consistency across all generated classes.
   - Never call a method or reference a class that is not defined in the Blueprint, generated files, or standard Java/Kafka libraries.

3. UNIFIED DTO & POJO MODEL:
   - DO NOT use Java records. Use standard Java POJO classes for all events and DTOs.
   - Every DTO/POJO must include: private fields, no-args constructor, all-args constructor, standard camelCase getters/setters, equals(), hashCode(), and toString().
   - Callers (Processors, Handlers, Tests) must strictly use these exact getter and setter method names.

4. SPRING BOOT & KAFKA BEST PRACTICES:
   - Use standard Spring annotations: @Service/@Component for Handlers and Suppliers, @Configuration for Kafka configuration, and constructor injection.
   - Use SLF4J (org.slf4j.Logger) for structured logging.
   - Configure JsonSerde or custom Serdes matching the respective DTO/event classes.

5. CONFIGURATION & TESTS:
   - For configuration files (e.g., application.yml), property keys must match the annotations or properties referenced in the Java code.
   - For test classes, write runnable JUnit 5 and Mockito tests that verify the exact methods and topics defined in the Blueprint.

6. FULL IMPLEMENTATION (NO PLACEHOLDERS):
   - Every class must contain complete, production-ready logic.
   - Do NOT use // TODO, // implement here, or dummy placeholders.
   - Do NOT use null to represent unimplemented code; handle optional or invalid data explicitly.

7. RESTRICTION:
   - Generate ONLY the files explicitly listed in FILES TO GENERATE NOW.
   - Do NOT declare multiple top-level classes in a single file.

OUTPUT FORMAT:
Return the complete code wrapped in XML tags:

<file name="ExactFileName.ext">
// Complete production-ready code
</file>

Repeat for every file listed in FILES TO GENERATE NOW.
Do NOT wrap XML tags in markdown code fences. Provide ONLY the <file> tags and their contents.
