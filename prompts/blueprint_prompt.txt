Design a Kafka Streams application based on these specs:
{spec_json}

Consider these retrieved patterns/standards:
{patterns_json}

The following files have already been generated by previous requests in the system across all tracks:
{existing_files}

Produce a JSON response with exactly this structure:
{{
  "files": [
    {{ "filename": "<ClassName>.java", "purpose": "<description of file purpose>", "generated": false, "status": "planned" }}
  ],
  "class_design": "Detailed string describing all generated classes and their specific responsibilities...",
  "rationale": "Why this design was chosen...",
  "alternative_designs": ["Alternative option 1...", "Alternative option 2..."],
  "assumptions": ["Assumed state store because...", "Assumed exact once semantics..."],
  "mermaid_diagram": "A valid Mermaid.js flowchart (graph TD) visualizing the Kafka streams topology. Strict rules: 1. Source topics at the top. 2. Processors in the middle. 3. Processors must connect horizontally to State Stores using database shapes, e.g. Processor <-->|Queries| Store[(State Store)]. 4. Show conditional branching from the processor to target topics (e.g. Processor -->|Valid| TargetTopic and Processor -->|Error| DLQTopic). Do not just draw a straight line. Use standard mermaid formatting without markdown backticks."
}}

Ensure you include ALL necessary files for a full production-ready Spring Boot Kafka Streams microservice based on the spec. This MUST include at minimum: pom.xml, the main Application.java class, configuration classes, data models/POJOs, the Processor (e.g. <ClassName>Processor.java), the Supplier (e.g. <ClassName>Supplier.java), the Handler (e.g. <ClassName>Handler.java), a JUnit test class (e.g. <ClassName>Test.java), and application.yml.
Do NOT copy the generic placeholder <ClassName>. Use the actual business entity name from the spec (e.g. EmployeeProcessor, PaymentSupplier, OrderHandler).

IMPORTANT FILE CACHING INSTRUCTION:
If you determine that a file required for this new project is ALREADY in the `existing_files` list, and that file requires ABSOLUTELY NO MODIFICATIONS for this new user story (e.g. standard configuration files, identical model classes), you MUST set its `"status": "reuse"`. The system will then automatically copy the existing code for that file. 
If the file is in the `existing_files` list but needs modifications for this specific request, or if it is a completely new file, you must set its `"status": "planned"`.

Respond ONLY with valid JSON.
