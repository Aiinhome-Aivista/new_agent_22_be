Design an application architecture and file manifest based on these specifications (which include Developer Input and Miro Diagram Facts):
{spec_json}

Consider these retrieved patterns/standards:
{patterns_json}

The following files have already been generated by previous requests in the system across all tracks:
{existing_files}

REWORK FEEDBACK (If applicable):
The following feedback has been provided on the previous iteration of this design. You MUST incorporate this feedback into your updated architecture and class design:
{comments}

UNIVERSAL ARCHITECTURE GENERATION PRINCIPLE:
Do not force a predefined architecture (e.g., Kafka/Event-Driven) onto the diagram. First, determine the architecture expressed by the source diagram. The architecture pattern, technology, component roles, communication style, and implementation structure must be derived from the provided inputs rather than assumed from a fixed template. The system must work across arbitrary domains and arbitrary diagram structures (E-Commerce, Banking, REST, Event-Driven, Microservices, etc).

CRITICAL ARCHITECTURE GENERATION PIPELINE:
You MUST follow this 4-layer priority model strictly. Never let a lower priority overwrite a higher priority.

PRIORITY 1: EXACT DIAGRAM FACTS (Highest Priority)
The visual diagram is the ABSOLUTE SOURCE OF TRUTH for the architecture topology.
- DIAGRAM FIDELITY HARD CONSTRAINT: Before generating any architecture, logically convert the source Miro diagram into a directed graph containing every visible node and every visible connector. 
- EXACT NODE RULE: Every Miro-visible node must appear in the Explicitly Visible section and in the generated topology exactly as labeled. Never rename an explicitly visible Miro component during extraction.
- EXACT EDGE RULE: Every Miro arrow must become an explicit source -> target relationship in the canonical blueprint.
- FAILURE RULE: Every FAIL node/edge must be preserved as an independent failure path. Never merge multiple failure paths into one generic error flow.
- NO LINEARIZATION: If the Miro diagram contains parallel branches, side paths, loops, or multiple failure routes, the generated topology MUST preserve those structures exactly. Never convert a branching workflow into a linear workflow.
- FALLBACK: If NO visual diagram is provided, use the explicit business flows defined in Developer Provided Facts or Architecture Standards.

PRIORITY 2: DEVELOPER PROVIDED FACTS
Any explicit technological choices provided by the developer must be applied to the nodes extracted in Priority 1.

PRIORITY 3: ARCHITECTURE STANDARDS
Apply retrieved architecture standards (patterns, validation, error handling) AS CONSTRAINTS to the nodes from Priority 1.
- IMPLEMENTATION RULE: Processor, Supplier, Handler, StateStore, RetryHandler, DLQ, IntegrationComponent, Service, Detector, etc. must NOT replace Miro nodes. They may only be introduced as separate implementation mappings under Developer Provided, Architecture Standard Applied, or AI Inference.

PRIORITY 4: AI INFERENCE (Lowest Priority)
Any inferences you make MUST NOT replace explicit facts and must be clearly documented as inferences. Never present AI inferences as explicit architectural facts.

REQUIRED INFRASTRUCTURE FILES:
You should include necessary infrastructure files to make the application runnable (if building a Java/Spring app):
- pom.xml
- main Application.java
- configuration files (application.yml, KafkaConfig, etc.)
- JUnit test classes

IMPORTANT FILE CACHING INSTRUCTION:
If a file required for this project is ALREADY in the existing_files list, and requires ABSOLUTELY NO MODIFICATIONS, set its "status": "reuse".
If the file is in existing_files but needs modifications, or if it is a new file, set "status": "planned".

JSON OUTPUT FORMAT:
Respond ONLY with valid JSON.
Produce a JSON response with exactly this structure:
{{
  "files": [
    {{ "filename": "<ClassName>.java", "purpose": "<description of file purpose>", "generated": false, "status": "planned" }}
  ],
  "class_design": "Detailed string describing all generated classes and their specific responsibilities. MUST break this down into 4 clear sections using these exact text labels (do NOT use '#' symbols):\nA. Explicitly Visible in Diagram (Priority 1):\nB. Developer Provided (Priority 2):\nC. Architecture Standard Applied (Priority 3):\nD. AI Inference (Priority 4):",
  "rationale": "Why this design was chosen...",
  "alternative_designs": ["Alternative option 1...", "Alternative option 2..."],
  "assumptions": ["List all AI inferences here clearly marked as assumptions."],
  "mermaid_diagram": "A valid Mermaid.js flowchart (graph TD) visualizing the exact topology. MUST faithfully reproduce every node, branch, parallel path, and failure path from the visual diagram (Priority 1). DO NOT convert a branching diagram into a single linear sequence. Preserve the exact topological position and relationships. Use standard Mermaid formatting without markdown backticks."
}}