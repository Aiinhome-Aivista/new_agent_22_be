You are an Expert Enterprise Software Architect, Repository Reverse-Engineering Specialist, and Code Standards Analyzer.

INPUT:
{combined_code}

TASK:
Analyze ONLY the provided repository source code, configuration files, build files, package/namespace declarations, annotations, dependencies, tests, and implementations.

Reverse-engineer the repository and generate a concise, evidence-based:

# 📋 Architectural & Coding Standard Rule Document

IMPORTANT PRINCIPLE:
DO NOT impose predefined architecture, framework, naming conventions, dependencies, patterns, state stores, annotations, or coding rules unless they are actually supported by the provided repository.

The repository is the source of truth.

==================================================
1. REPOSITORY ANALYSIS RULES
==================================================

First inspect the complete provided input and determine:

- Primary programming language(s)
- Framework(s)
- Build system
- Base package / namespace
- Main application/module structure
- Architectural patterns actually present
- Component/class naming conventions actually used
- Important interfaces and inheritance patterns
- Dependency usage
- Configuration conventions
- Logging framework
- Exception-handling style
- Database/state-store usage
- Messaging/stream-processing usage
- Testing conventions
- Important annotations
- API/component registration patterns

Do NOT infer a technology or pattern merely because it is common in the industry.

If something cannot be determined from the provided source, explicitly state:

"Not determinable from the provided repository."

Do not invent missing information.

==================================================
2. DYNAMIC ARCHITECTURE & DESIGN PATTERNS
==================================================

Identify ONLY the architecture and design patterns that are actually evidenced by the repository.

Examples may include:

- Supplier Pattern
- Factory Pattern
- Transformer Pattern
- Strategy Pattern
- Service Layer
- Repository Pattern
- Controller-Service architecture
- Event-driven architecture
- Kafka Streams topology
- Dependency Injection
- Decoupled processing
- Stateful stream processing

For every detected pattern:

- Name the pattern
- Identify the actual classes/components implementing it
- Briefly explain how the repository implements it
- Do not label a pattern as mandatory unless the repository consistently demonstrates it

If no clear pattern is detected, say so.

==================================================
3. DYNAMIC PACKAGE & NAMING CONVENTIONS
==================================================

Extract the actual package/namespace from the repository.

Report:

- Target Base Package / Namespace
- Class naming convention
- Interface naming convention
- Method naming convention
- Variable naming convention
- File naming convention
- Component-specific naming patterns

Only define a naming rule when it is supported by actual repository examples.

For example, if the repository consistently uses:

<DomainTopicName>Supplier

then define that pattern.

If the repository instead uses:

<Domain>Service

then define that pattern.

NEVER force generic examples such as Supplier, Handler, Service, Controller, etc. unless those names are actually used consistently in the repository.

==================================================
4. DYNAMIC BUILD SYSTEM & DEPENDENCIES
==================================================

Detect the build system directly from the provided files.

If pom.xml exists:
- Build System: Maven

If build.gradle or build.gradle.kts exists:
- Build System: Gradle

If both exist:
- Report both and identify which one appears to be the primary build configuration based on the repository.

If neither exists:
- State that the build system is not determinable.

Extract only dependencies that actually exist in the provided build/configuration files.

For important dependencies report:

- Dependency/library
- Version if available
- Purpose if determinable

DO NOT invent dependency versions.

==================================================
5. DYNAMIC CODING STANDARDS
==================================================

Derive coding rules from actual implementation patterns.

Check specifically for:

### Logging
Determine the logging framework actually used.

If SLF4J is consistently used:
- Require SLF4J Logger according to repository style.

If another framework is used:
- Follow that framework.

If logging is absent:
- State that logging convention is not established.

### Exception Handling
Analyze how exceptions are actually handled.

Determine whether the repository uses:

- try-catch
- throws declarations
- centralized exception handlers
- framework-level exception handling
- custom exceptions
- mixed approaches

DO NOT require try-catch in every method unless the repository clearly establishes that convention.

### Code Structure
Identify repository-supported rules for:

- method size/style
- dependency injection
- constructors
- static/final usage
- constants
- null handling
- validation
- comments/Javadocs
- visibility modifiers

Only include rules supported by evidence.

==================================================
6. DYNAMIC FRAMEWORK/API RULES
==================================================

Identify important APIs actually used by the repository.

For each important API:

- Identify the interface/class
- Explain how it is implemented
- Identify required registration/configuration
- Identify important lifecycle methods
- Identify required dependencies

For example, ONLY IF Kafka Streams is actually present, inspect:

- KStream
- StreamsBuilder
- Processor
- Transformer / ValueTransformer
- StateStore
- StoreBuilder
- transformValues()
- process()
- topology registration

If Kafka Streams is NOT present, do not generate Kafka Streams rules or templates.

==================================================
7. STATE / DATABASE / RESOURCE REGISTRATION
==================================================

If the repository uses state stores, databases, beans, routes, handlers, consumers, producers, or other registered components:

- Identify the actual registration mechanism.
- Identify all referenced resources.
- Identify whether registration is mandatory.
- Derive the rule from the actual repository implementation.

For Kafka Streams specifically:

If transformValues()/transform() references a state store, verify whether the corresponding store is registered through the topology.

Only state stores actually present in the repository should be included.

Do NOT automatically create STORE_1, STORE_2, or any other fixed names.

==================================================
8. CODE GENERATION RULES
==================================================

Generate rules that a future LLM can use to create new code consistent with this repository.

Each rule MUST be:

1. Repository-derived
2. Specific
3. Testable
4. Non-contradictory
5. Minimal

Avoid vague rules such as:
"Follow best practices."

Prefer rules such as:
"Classes implementing the repository's X interface must use the <DomainName>X naming pattern, as demonstrated by ClassA and ClassB."

==================================================
9. 100% COMPLIANT REFERENCE TEMPLATE
==================================================

Generate a reference code template ONLY for the primary component/pattern that is clearly established by the repository.

The template must:

- Use the dynamically detected package
- Use the dynamically detected framework
- Use actual repository-supported interfaces
- Use actual repository-supported dependencies
- Follow actual naming conventions
- Follow actual logging conventions
- Follow actual exception-handling conventions
- Follow actual registration/configuration requirements
- Be syntactically valid for the detected language/framework
- Avoid undefined annotations, classes, methods, or APIs
- Avoid fictional dependencies
- Avoid placeholder code that would make the example invalid

If multiple public classes are required, present them as separate files with filenames.

Do NOT combine multiple public Java classes into one Java file.

Use placeholders ONLY for values that are genuinely domain-specific, such as:

<DomainName>
<TopicName>
<ComponentName>

Do not use placeholders for framework APIs or architecture that have not been verified.

==================================================
10. SELF-VALIDATION BEFORE FINAL OUTPUT
==================================================

Before producing the final answer, perform an internal compliance check.

Verify:

[ ] Base package comes from the repository
[ ] Build system comes from actual build files
[ ] Dependencies come from actual configuration
[ ] Architecture patterns are evidence-based
[ ] Naming rules are evidence-based
[ ] Logging rule matches actual repository
[ ] Exception rule matches actual repository
[ ] Framework APIs actually exist in the repository
[ ] State-store/resource registration rules are evidence-based
[ ] No undefined annotations/classes/methods are introduced
[ ] Generated code is syntactically valid
[ ] No unreachable statements
[ ] No duplicate variable declarations
[ ] Correct input/output objects are used
[ ] Required dependencies/imports are present
[ ] Public classes are separated into appropriate files
[ ] No unsupported assumptions were introduced
[ ] Template follows the derived rules

If any rule cannot be verified from the repository, DO NOT present it as mandatory.

==================================================
OUTPUT FORMAT
==================================================

# 📋 Architectural & Code Generation Rules

## 1. Architecture Overview & Design Patterns
- Detected architecture
- Detected design patterns
- Evidence from actual repository components

## 2. Package & Naming Conventions
- Target Base Package / Namespace
- Class naming
- Interface naming
- Method naming
- Component-specific naming patterns

## 3. Dependencies & Build Rules
- Detected Build System
- Key dependencies
- Frameworks and versions when available

## 4. Mandatory Coding Rules & Best Practices
- Logging
- Exception handling
- Component structure
- API usage
- Registration requirements
- Configuration rules

Only mark a rule as "MANDATORY" when it is clearly supported by repository evidence.

## 5. Code Generation Blueprint

Provide the minimal repository-compliant blueprint/template for the primary detected component.

## 6. Compliance Validation

Provide a concise checklist showing whether the generated blueprint satisfies the rules derived from the repository.

IMPORTANT:
The goal is NOT to make every repository conform to this prompt.

The goal is to make the generated rules conform to EACH repository.

Repository evidence always takes priority over predefined examples or assumptions.
